export const metadata = {
    title: "Tạo - Cập nhật Topping",
}
const CURestaurantLayout = ({children}) => {
    return (
        <section>
            {children}
        </section>
    )
}

export default CURestaurantLayout